[build-system]
requires = [
    "setuptools>=42",
    "wheel",
    "clingo",
    "numpy",
    "scipy",
    "sympy",
    "pytest",
    "statistics",
    "gekko",
    "pytest-rerunfailures"
]
build-backend = "setuptools.build_meta"
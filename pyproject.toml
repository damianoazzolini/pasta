[build-system]
requires = [
    "setuptools>=42",
    "wheel",
    "clingo",
    "numpy",
    "scipy",
    "sympy",
    "pytest",
    "statistics",
    "gekko"
]
build-backend = "setuptools.build_meta"